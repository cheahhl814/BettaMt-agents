// ---------------------------------------------------------------------------
// Local-executor profile (laptop / workstation)
// ---------------------------------------------------------------------------
profiles {
    local {
        process { executor = 'local' }
    }
}
